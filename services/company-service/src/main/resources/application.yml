server:
  port: 8081

spring:
  profiles:
    active: dev
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: "http://localhost:8888"
#      discovery:
#        enabled: true
#        service-id: config-server

  application:
    name: company-service
