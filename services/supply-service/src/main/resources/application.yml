server:
  port: 8083

spring:
  profiles:
    active: dev
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: "http://localhost:8888"

  application:
    name: supply-service


