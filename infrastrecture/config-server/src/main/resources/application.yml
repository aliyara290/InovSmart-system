server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: ${HOME}/Documents/Java/Spring-Boot/InovSmart-config-repo/config-repo